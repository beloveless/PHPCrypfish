version: "3.8"
services:
  phpcrypfish-apache:
    image: phpcrypfish:latest
    container_name: phpcrypfish-apache
    ports:
      - "80:80"
    networks:
      - my-network

networks:
  my-network:
    driver: bridge